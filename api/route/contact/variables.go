package contact

var (
	// CreateContact : the function to create a contact.
	CreateContact CreateContactFunc

	// UpdateContact : the function to update a contact.
	UpdateContact UpdateContactFunc

	// GetContacts : the function to get the contacts.
	GetContacts GetContactsFunc

	// GetContact : the function to get a contact.
	GetContact GetContactFunc
)
