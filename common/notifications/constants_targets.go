package notifications

const (
	// TargetEmail : receives the notifications by emails.
	TargetEmail = "email"

	// TargetFirefox : receives the notifications by navigator firefox.
	TargetFirefox = "firefox"

	// TargetSafari : receives the notifications by navigator safari.
	TargetSafari = "safari"

	// TargetChrome : receives the notifications by navigator chrome.
	TargetChrome = "chrome"

	// TargetIOS : receives the notifications by app iOS.
	TargetIOS = "ios"

	// TargetAndroid : receives the notifications by app android.
	TargetAndroid = "android"
)
